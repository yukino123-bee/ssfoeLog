{
    "name": "home/ssfo",
    "authors": [
        {
            "name": "Dev Bot",
            "email": "devnull@example.com"
        }
    ],
    "require": {
        "setasign/fpdf": "^1.8",
        "setasign/fpdi": "^2.6",
        "phpoffice/phpword": "^1.4"
    },
    "scripts": {
        "setup": [
            "@composer install",
            "@php -r \"foreach (['storage/logs', 'storage/rate_limits', 'public/uploads/requests'] as $dir) { if (!is_dir($dir)) { mkdir($dir, 0755, true); } }\""
        ],
        "start": "@php -S 127.0.0.1:8080 -t public public/router.php",
        "serve": "@start",
        "check": "find app public routes -type f -name '*.php' -print0 | xargs -0 -n1 php -l"
    }
}
